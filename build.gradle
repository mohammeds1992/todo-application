apply plugin: 'java-library'

repositories {
    jcenter()
}

jar {
    manifest {
        attributes(
                'Main-Class': 'app.main.todo.driver.Driver'
        )
    }
}

dependencies {

    testImplementation 'junit:junit:4.12'
}